const prom = new Promise((resolve, reject) => {
  resolve('Yay!');
});

// const handleSuccess = (resolvedValue) => {
//   console.log(resolvedValue);
// };

function xd(res) {
  console.log(res);
}

// prom.then(res => {
//   console.log(res);
// })

// console.log(prom)
// prom.then(xd)

const test = (a => {
  a = {
    "Name": "Nothing"
  }
  return a
})
test
